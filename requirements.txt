# Python dependencies for the inference server + Pipecat server.
# Install into a virtualenv:  python -m venv .venv && .venv/bin/pip install -r requirements.txt
# Versions below match what this project was developed and tested against.

# --- Inference server (Qwen3-TTS model) ---
torch==2.12.0
transformers==4.57.3
numpy==2.4.6
soundfile==0.13.1
fastapi==0.136.3
uvicorn==0.48.0
websockets==16.0

# --- Pipecat server (voice pipeline) ---
# The extras pull in: aiortc (SmallWebRTC), Deepgram STT, Silero VAD, the runner.
pipecat-ai[webrtc,deepgram,silero,runner]==1.3.0
openai==2.38.0
python-dotenv==1.2.2

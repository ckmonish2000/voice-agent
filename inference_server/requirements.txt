# Inference server dependencies (Qwen3-TTS streaming server).
# This is the subset the server itself needs — NOT the Pipecat client deps
# (those live in the top-level requirements.txt).
#
# Install with uv (fast) or pip:
#   uv pip install -r requirements.txt          # into the active env
#   uv pip install --system -r requirements.txt # into the system env (Vast box)
#   pip install -r requirements.txt             # plain pip
#
# Run:  cd inference_server && \
#       LDG_VOCAB_SIZE=3072 USE_KERNEL=1 python -m uvicorn app:app --host 0.0.0.0 --port 8000

# --- model + runtime ---
# qwen-tts brings the Qwen3-TTS model + the qwen3_tts arch the engine loads,
# and pulls accelerate/librosa/torchaudio/onnxruntime/etc. transitively.
qwen-tts==0.1.1
torch>=2.8                 # bf16 + sm_120a (RTX 5090) need a recent CUDA build
transformers==4.57.3
numpy
soundfile==0.13.1

# --- web server ---
fastapi==0.136.3
uvicorn==0.48.0
websockets==16.0

# --- megakernel JIT build (only needed when USE_KERNEL=1) ---
# ninja is required by torch.utils.cpp_extension to compile csrc/kernel.cu.
ninja
